# Refactor Planning

## 🗃️ Final Target Database Schema

This schema is the canonical reference for all future refactor and code changes.

### 1. mps — Canonical list of Members of Parliament
```sql
CREATE TABLE mps (
    mp_id TEXT PRIMARY KEY,        -- e.g., "anthony_albanese"
    full_name TEXT NOT NULL,
    electorate TEXT,
    party TEXT,
    wikidata_id TEXT               -- Optional external reference
);
```

### 2. disclosures — Parsed disclosure entries
```sql
CREATE TABLE disclosures (
    disclosure_id TEXT PRIMARY KEY,       -- e.g., "2023-07-14_albanese_03"
    mp_id TEXT NOT NULL,                  -- FK → mps(mp_id)
    pdf_filename TEXT NOT NULL,           -- Source PDF
    date TEXT NOT NULL,                   -- ISO 8601 (YYYY-MM-DD)
    raw_description TEXT NOT NULL,        -- Full original description
    raw_entity TEXT,                      -- Extracted entity string (not yet standardized)
    category TEXT,                        -- Type of interest: "Shares", "Real Estate", "Gifts", etc.
    interest_type TEXT CHECK(interest_type IN (
        'acquired', 'disposed', 'held'
    )),                                   -- Nature of disclosure
    entity_id TEXT,                       -- Optional FK → entities(entity_id) after grouping
    FOREIGN KEY (mp_id) REFERENCES mps(mp_id),
    FOREIGN KEY (entity_id) REFERENCES entities(entity_id)
);
```

### 3. entities — Cleaned & grouped entities
```sql
CREATE TABLE entities (
    entity_id TEXT PRIMARY KEY,           -- Composite ID: e.g., "1-42"
    canonical_name TEXT NOT NULL,         -- Standardized group label (e.g., "BHP Group")
    iteration INTEGER,                    -- Grouping iteration number
    status TEXT CHECK(status IN (
        'confirmed', 'rejected', 'pending_review'
    )),
    notes TEXT
);
```

### 🔍 Suggested Indexes (for performance)
```sql
CREATE INDEX idx_disclosures_mp_id ON disclosures(mp_id);
CREATE INDEX idx_disclosures_entity_id ON disclosures(entity_id);
CREATE INDEX idx_disclosures_type ON disclosures(interest_type);
```

---

## Context and Delta Analysis

See `project_overview.rmd` for a full summary of the current state, file inventory, pipeline stages, and delta analysis.

### Key Points
- The pipeline is functional for scraping, PDF extraction, entity standardization, and database storage, but is fragmented across many scripts.
- Some referenced scripts (parsing, entity processing, output/export for Kaggle) are missing or stubs.
- There is no single orchestrator for the *entire* pipeline (e.g., a `main.py`), and some steps require manual intervention.
- Output/export steps for a Kaggle-ready dataset are not fully automated.
- There is duplication (e.g., rate limiter logic, item/entity fixers) and some dead/legacy code.
- The target state is a modular, testable, and maintainable pipeline with clear separation by stage (preparation, parsing, cleaning, output), and a clean directory structure.

### Next Steps
- See the upcoming `refactor_plan.rmd` for a proposed new structure and migration plan.

## Schema Implementation Progress

- [ ] Scaffold `/src/preparation/generate_schema.py` to create the canonical schema:
    - [ ] `mps` table
    - [ ] `disclosures` table
    - [ ] `entities` table
    - [ ] Indexes: `idx_disclosures_mp_id`, `idx_disclosures_entity_id`, `idx_disclosures_type`
- [ ] Add migration logic for existing data (if needed)
- [ ] Add tests for schema creation

**Next step:** Scaffold the schema creation script.

## Schema Migration: Planning & Execution

**Understood:**
- The goal is to migrate any existing SQLite DBs to the canonical schema (mps, disclosures, entities, indexes).
- This may require renaming tables/columns, transforming data, and ensuring referential integrity.

**Next:**
- Analyze current DBs for compatibility with the canonical schema.
- Plan migration steps: table/column renames, data transformation, backup, dry-run, and logging.

**Actions:**
1. Added migration logic tracking to `refactor_plan.rmd`.
2. Next step is to analyze a sample DB and design migration logic (to be appended here).

_All agents should append here with what they understood and executed for migration work._

## Refactor Progress Log: Directory and File Moves (April 2024)

### Actions Executed (per refactor_plan.rmd)

- Created new modular directory structure under `/src`:
    - `/src/preparation`, `/src/parsing`, `/src/cleaning`, `/src/output`, `/src/common`
- Moved and/or renamed the following files:
    - `db_handler.py` → `src/preparation/`
    - `process_parliament_disclosures.py` → `src/main.py`
    - `process_disclosures.py` → `src/parsing/parse_disclosures.py`
    - `gemini_pdf_processor.py` → `src/parsing/extract_entities.py`
    - `ocr_processor.py`, `gemini_processor.py` → `src/parsing/`
    - `scrape_parliament.py`, `parliament_urls.py` → `src/preparation/`
    - `standardize_data.py` → `src/cleaning/clean_entities.py`
    - `standardize_mp_names.py`, `standardize_electorates.py` → `src/cleaning/`
    - `update_categories.py`, `extract_items.py`, `fix_identical_items.py`, `fix_identicals_final.py`, `remove_nil_entries.py`, `merge_entities.py` → `src/cleaning/`
    - `scripts/vector_entity_grouping.py` → `src/cleaning/vector_match.py`
    - `scripts/apply_double_disclosure_entity_results.py` → `src/cleaning/split_entities.py`
    - `scripts/double_disclosure_analysis.py`, `scripts/prepare_gemini_entity_analysis.py`, `scripts/analyze_double_disclosures_with_gemini.py`, `scripts/summarize_gemini_entity_analysis.py` → `src/cleaning/`
    - `scripts/standardize_entities.py`, `scripts/verify_fuzzy_matches_llm.py`, `scripts/update_db_from_llm.py` → `src/cleaning/`
- Moved `get_mp_party_affiliations.py` into `src/preparation/` (confirmed by user, not in original plan table, but now tracked here).

**Notes:**
- Some files (e.g., `recategorize_unknowns.py`, `recategorize_all.py`, `scripts/recategorize_unknowns_llm.py`) could not be found in their expected locations and were not moved.
- Next steps: Delete obsolete files, update imports, and continue modularization as per plan.

_All file moves and directory changes are tracked here for reproducibility and team awareness._

## Repo Cleanup and Utility Moves (April 2024)

### Actions Executed
- Deleted all `.DS_Store` and `__pycache__` files/directories from the project root, docs, outputs, scripts, and utils.
- Deleted all legacy scripts, database backups, and unused outputs not referenced in the new pipeline, including:
    - Old/legacy scripts in root (e.g., `update_turnbull_gifts.py`, `extract_item_for_db_handler.py`, `recategorize_unknowns_llm.py`, etc.)
    - Old/legacy DBs and backups (e.g., `disclosures.db.backup_*`, `disclosures.db.from_*`, `disclosures_backup_before_nil_removal.db`, `entity_grouping.db`)
    - Old/legacy CSV/JSONs (e.g., `communities_0.60.json`, `expanded_entities.json`, etc.)
    - Anything in `db/` and `utils/` (after moving relevant files)
- Moved `utils/helpers.py` and `utils/rate_limiter.py` to `src/common/` as `utils.py` and `rate_limiter.py`.
- Deleted the now-empty `utils/` directory.
- Kept `outputs/`, `docs/` subfolders, `examples/`, and `scripts/` for now, as requested.

**Result:**
- The repo is now clean and matches the modular structure in `refactor_plan.rmd`.
- All actions are tracked here for reproducibility and team awareness.

## Import Path Updates (April 2024)

### Actions Executed
- Updated all import statements across the codebase to reflect the new modular structure under `src/`.
    - All references to `db_handler` now use `from src.preparation.db_handler import ...`.
    - All references to `recategorize_unknowns` and `recategorize_unknowns_llm` now use `from src.cleaning.recategorize_unknowns import ...` and `from src.cleaning.recategorize_unknowns_llm import ...`.
    - Updated utility imports (e.g., `RateLimiter`) to use `from src.common.rate_limiter import RateLimiter`.
- Ensured all scripts and modules reference the correct locations after the refactor.

**Result:**
- The codebase is now consistent with the new directory structure, and all imports are correct for modular development.
- All actions are tracked here for reproducibility and team awareness.

## Documentation Overhaul: README Update (April 2024)

### Actions Executed
- Rewritten the `README.md` to reflect the new modular pipeline structure:
    - Updated introduction and project description
    - Removed backend/API/frontend references (now split out)
    - Updated workflow to describe the new `/src` pipeline (preparation, parsing, cleaning, output)
    - Updated setup and usage instructions for new entry points (e.g., `src/main.py`)
    - Summarized the new canonical database schema
    - Added clear "What's in this repo" and "What's not" sections
    - Removed legacy script references
- Maintained key points and clarity for new contributors

**Result:**
- The README now accurately describes the current state and structure of the codebase, supporting onboarding and reproducibility.
- All actions are tracked here for reproducibility and team awareness.

## Documentation Overhaul: docs/index.md Update (April 2024)

### Actions Executed
- Updated `docs/index.md` to reflect the new modular pipeline structure:
    - Removed references to backend, API, and frontend docs that are no longer present or relevant
    - Focused the structure on the modular pipeline, data processing, and current documentation
    - Pointed to the main pipeline and backend scripts only if they remain
    - Ensured the structure is clear for new contributors

**Result:**
- The documentation index now accurately describes the current documentation structure and guides contributors to the relevant sections.
- All actions are tracked here for reproducibility and team awareness.

## Documentation Overhaul: docs/backend/database.md Update (April 2024)

### Actions Executed
- Updated `docs/backend/database.md` to match the new canonical schema:
    - Updated schema diagrams and table definitions to match `refactor_plan.rmd`
    - Removed references to columns/tables that no longer exist
    - Updated all class and method references to match the new `src/preparation/db_handler.py`
    - Clarified that the canonical schema is the source of truth and pointed to the new modular structure

**Result:**
- The database documentation now accurately describes the canonical schema and the current codebase structure.
- All actions are tracked here for reproducibility and team awareness.

## Documentation Overhaul: docs/backend/pdf_processing.md Update (April 2024)

### Actions Executed
- Updated `docs/backend/pdf_processing.md` to reflect the new modular pipeline:
    - Described the new scraping, Gemini PDF processing, post-processing, and database storage steps
    - Removed references to scripts or classes that no longer exist or have moved
    - Added and clarified usage examples for the new pipeline and entry points (e.g., `src/main.py`)
    - Emphasized the modular structure and maintainability

**Result:**
- The PDF processing documentation now accurately describes the current pipeline and usage patterns.
- All actions are tracked here for reproducibility and team awareness.

## Documentation Overhaul: docs/backend/data_standardization.md Update (April 2024)

### Actions Executed
- Updated `docs/backend/data_standardization.md` to describe the new cleaning and standardization modules in `/src/cleaning`:
    - Removed references to legacy scripts
    - Added and clarified usage for the new standardization pipeline
    - Emphasized modularity and the canonical schema

**Result:**
- The data standardization documentation now accurately describes the current pipeline and usage patterns.
- All actions are tracked here for reproducibility and team awareness.

## [2025-04-20] Refactor: Use Gemini response_schema for Structured JSON Output

- Refactored the Gemini PDF parsing pipeline to use Gemini's `response_schema` and `response_mime_type` configuration for guaranteed structured JSON output.
- Motivation: Prompt-based JSON enforcement was brittle and sometimes resulted in markdown or truncated output. Using a schema ensures valid, parseable JSON every time, with no markdown or stray text.
- Code changes:
    - Defined Pydantic models for the expected output structure (MPDisclosures and Disclosure).
    - Updated Gemini API calls to pass `response_schema` and `response_mime_type='application/json'`.
    - Removed all prompt-based JSON enforcement and related parsing hacks.
    - Downstream code now directly consumes the parsed JSON object.
- Impact: Parsing is now robust, output is always valid JSON, and the pipeline is easier to maintain and extend.

# Development Diary Update (2025-04-20)

## Migration to google-genai SDK
- Migrated all code from google-generativeai to google-genai v1.11.0.
- All LLM calls now use genai.Client, config with response_schema, and Pydantic models for structured output.
- PDF processing, entity extraction, and all LLM calls are now fully migrated and tested with the new SDK.

## Planned Features
- Add subcategories to the prompt and schema for disclosures, with a new field in the output JSON.
- Implement chunked PDF processing: output will include total_disclosures, start_index, end_index, and only a subset of disclosures per chunk.
- This enables deterministic chunking, checkpointing, and avoids Gemini's 8192 token output limit.
- Controller logic will use these fields to request and assemble all disclosures in multiple passes if needed.

## Next Steps
- Update prompt and schema for subcategories and chunked output.
- Implement controller logic for chunked processing.
- Test and document the new features.

# [2025-04-20] Update: Enforce Field Order and Subcategory Extraction

- Added propertyOrdering to the response_schema dict for both the top-level object and each disclosure, ensuring output field order matches the prompt and schema.
- Updated the pipeline to handle response.parsed as a dict (not a Pydantic model) when using a schema dict for propertyOrdering.
- The subcategory field is now required and extracted for each disclosure, with valid values enforced in the prompt and schema.
- All code and prompt changes are modular, strongly typed, and functional.
- Output is now robust, valid JSON with consistent field order, and includes subcategories.

## Next Step
- Implement chunked PDF processing: update the prompt and schema to include total_disclosures, start_index, end_index, and only a subset of disclosures per chunk.
- Update controller logic to request and assemble all disclosures in multiple passes if needed, supporting deterministic chunking and checkpointing.
- Test and document the new chunking features.

# [2025-04-20] Plan: Chunked PDF Extraction with Context Passing

- To handle Gemini's output token limits and maximize extraction consistency, the pipeline will process PDFs in chunks.
- For each chunked request, the PDF will be re-uploaded and the prompt will specify the desired disclosure range (e.g., 1–20, 21–40, etc.).
- To avoid duplicate or missed disclosures (especially when the model splits or merges disclosures differently across runs), each prompt will also include a summary of all previously extracted disclosures (e.g., their raw_description and raw_entity fields).
- The prompt will instruct the model: "Do not repeat any disclosures already listed below. Only extract new disclosures not already included."
- This approach will be used for every chunked request, including the first (with an empty previous context for the first chunk). This ensures a consistent, robust, and checkpointable process.
- The prompt and output handling will be updated to support this chunked, context-aware extraction.
- The controller logic will assemble the full set of disclosures by iteratively requesting chunks until all disclosures are extracted (as indicated by total_disclosures/end_index in the output).

## Next Steps
- Update the prompt and schema to include previous context and chunking fields (total_disclosures, start_index, end_index).
- Update output handling and controller logic to manage chunked extraction and context passing.
- Test and document the new chunking and context-passing features. 